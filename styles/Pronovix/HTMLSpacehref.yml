extends: existence
message: "Don't use space in a href tag, use hyphens instead"
ignorecase: false
nonword: true
level: error
tokens:
  - '<a href="[a-zA-Z0-9?:=./\-#@()*;+$,%\[\]_]*[ ]+[a-zA-Z0-9?:=./\-#@()*;+$,%\[\]_]*"'